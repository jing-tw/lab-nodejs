{
  "name": "ble",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf out build dist node_modules & rimraf **/*.js & rimraf **/*.map & rimraf **/*.raw & rimraf **/*.log",
    "start": "tsc && cd src && node 01-test-ble-device-basic"
  },
  "dependencies": {
    "@abandonware/noble": "^1.9.2-11",
    "winston": "^3.3.3",
    "winston-transport-browserconsole": "^1.0.5"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
