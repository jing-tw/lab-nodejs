{
  "name": "client",
  "version": "1.0.0",
  "main": "client.js",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^15.6.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "tsc & node src/client.js",
    "clean": "rimraf out build dist node_modules & rimraf **/*.js & rimraf **/*.map & rimraf **/*.raw & rimraf **/*.log"
  },
  "devDependencies": {
    "glob": "^7.1.7",
    "rimraf": "^3.0.2"
  }
}
